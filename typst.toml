[package]
name = "casemiromjm-academic-report"
version = "0.1.0"
compiler = "0.10.0"
entrypoint = "lib.typ"
repository = "https://github.com/casemiromjm/report-template"
authors = ["Casemiro Melo <https://casemiromjm.me>"]
license = "MIT-0"
description = "Typst template reports done in academic context."
keywords = ["feup", "academic", "report", "clean"]
categories = ["report"]

[template]
path = "template"
entrypoint = "main.typ"
thumbnail = "thumbnail.png"
